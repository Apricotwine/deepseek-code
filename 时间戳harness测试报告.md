# 时间戳 Harness 测试报告

> 2026-08-06 · 来源：IDE 自评观测 + Codex 修复与矩阵复测
> 相关文件：[benchmarks/](benchmarks/)（large 基准、矩阵探针、cache probe）

## 一、结论摘要

时间感知层（TAL）的**有效性已被大规模数据确认**（72 用例 × 双模型），但自评同时
抓到了 3 个真实缺陷，已修复 2 个、部分修复 1 个。最有价值的产出是**决策链变体
（T1-dc）**：它把"标签翻转"下的鲁棒性从 ~26% 拉高到 98.6%，直接解决了论文里
"模型只跟标签不推理年龄"的短板。

## 二、当场抓到的四个观察点与处置

| # | 观察 | 判定 | 处置 |
|---|---|---|---|
| ① | 系统提示里有一个冻结的过期时钟（15:30），与消息级时钟（16:31）冲突 | 真实缺陷 | **已修复**：系统提示不再携带时钟，收敛为单一权威时钟（消息级 `[time_harness now=...]`） |
| ② | web_search 结果没有 `data_time` 标注 | 真实缺陷 | **已修复**：服务端搜索结果统一打 `[data_time=... horizon=24h]` |
| ③ | 调用级新鲜 ≠ 内容级新鲜（moji 页面 2017 年缓存但搜索是新的） | 设计盲区 | **部分修复**：系统规则新增"内容自带时间戳优先"；`web_fetch` 增加内容级嗅探（相对时间、"X分钟前"、epoch 缓存参数） |
| ④ | 文件 mtime 与 1h 规则自洽 | 确认无问题 | 顺手修复：`read_file` 的 stamp 原写 `horizon=30min`，与"文件无保鲜期"规则冲突，改为 `horizon=none` |

### ① 时钟收敛（关键修复）

旧设计：系统提示在会话初始化时嵌入 `now`，之后每轮不再刷新 → 会话超过 1 小时就
出现双时钟。修复后系统提示完全静态（缓存友好），唯一权威时钟是每轮请求尾部由
`stamp_messages_for_wire` 生成的 `[time_harness now=...]`。

矩阵探针 T5 验证：植入两个差 1 小时的时钟时，模型识别出冲突并选择消息级时钟为
权威（`{"time":"16:31:33","conflict":true,"authoritative":"message_stamp_clock"}`）。

### ③ 内容级新鲜度（v1）

- 系统规则：搜索结果可能比抓取时间更旧；内容自带时间戳（"21分钟前"、发布日期、
  缓存参数）与抓取时间冲突时，以内容证据为准并说明。
- `web_fetch` 新增 `[content_time_hint=...]`：识别中文/英文相对时间短语与
  URL/文本中的 13 位 epoch 缓存参数（如 `r=1504921726666` → 2017 年）。

## 三、测试矩阵结果（本次复测，T5-T8）

| 探针 | 结果 |
|---|---|
| T5 时钟一致性（双时钟冲突） | ✅ 全过：发现冲突、选对权威时钟 |
| T6 假新鲜陷阱（2017 缓存标 fresh） | ⚠️ 带 harness 与基线**都**识别出陈旧——模型模式识别很强，harness 规则是冗余保险 |
| T7 混合保鲜期（股价30min+网页24h+文件无过期） | ✅ 只重取股价 `[1]`，分层应用正确 |
| T8 陈旧自纠错（31h 包裹结论，horizon 6h） | ✅ 自动触发 refetch，理由正确 |
| T9 时区健壮性（+0800 与美股 ET 跨日期翻转，4 例） | ✅ 带 harness **100%**（含"同日 10:00 实则隔天"陷阱）；基线 75%（唯一错误仍是"总是重取"偏置） |

探针结果文件：`benchmarks/temporal_matrix_v2_probes.json`（T5-T9 完整回复与判定）。

> 修探针实录：T9 首轮 with-stack 只到 75%，排查后发现是 `max_tokens=80` 把
> "推理一行 + 结论一行"的回复截断、解析回退成 no_tool——**是探针 bug，不是
> 模型错误**；加大 token 并改为取最后一行判定后回到 100%。

## 四、大规模基准（IDE 自评产出，72 用例/探针，双模型）

数据源：`benchmarks/temporal_harness_result_large_flash.json` /
`temporal_harness_result_large_pro.json`

| 探针 | Flash 基线 → harness | Pro 基线 → harness |
|---|---:|---:|
| T1 工具调用时机（72 例） | 0.625 → **0.917** | 0.431 → **1.000** |
| T1 标签翻转对照（72 例） | 0.264 | 0.306 |
| T1-dc 决策链（72 例） | **1.000** | **1.000** |
| T1-dc 标签翻转 | **0.986** | **0.986** |
| T3 时间一致性（72 例） | 0.931 → 0.958 | 0.931 → 0.958 |
| T2 自测时长（9 例） | 中位 **3.71×**（高估） | 中位 **0.56×**（低估） |

**两个关键结论：**

1. **决策链变体消灭了标签依赖**：让模型先"自己算 age vs horizon"再下结论，
   正确标签 100%、翻转标签 98.6%。这就是论文"未来工作"里写的 age reasoning，
   已被实证解决。
2. **校准系数必须按模型区分**：Flash 高估 3.71×、Pro 低估 0.56×，固定 3.85
   系数只对 Flash 成立。已改为 per-model 校准。

## 五、缓存命中实测（IDE 自评产出）

数据源：`benchmarks/cache_probe_short.json` / `cache_probe_long.json`

| 条件 | 短消息（300 字符）二次调用 cache 占比 | 长消息（1200 字符） |
|---|---:|---:|
| 无 TAL | 96.5% | 98.0% |
| 有 TAL（分桶年龄 + 尾部时钟） | **99.4%** | **99.5%** |

论文"缓存感知放置"的设计主张得到了实证：分桶年龄让前缀在桶内字节稳定，时钟只
在尾部刷新，**TAL 不牺牲前缀缓存，甚至略高**。

## 六、剩余边界

- 内容级新鲜度目前是规则 + 嗅探提示，还没有"页面级 horizon"（如：餐馆价目表每周
  变、历史网页几年不变）的自动推断。
- 决策链变体（T1-dc）已集成进生产系统提示（见下），但目标计划步骤的决策链尚未
  单独验证。
- T3 增益偏小（0.931→0.958）：时间一致性审计对 Flash/Pro 来说基线已经不弱，
  需要更难的合成场景拉开差距。

## 七、建议下一步

1. ✅ 决策链提示词已集成进生产系统提示（`context.rs::time_harness_system_section`：
   自己算 age、标签可能错、日期跨时区翻转别上当）。
2. ✅ per-model 校准系数（Flash 3.71 / Pro 0.56）已更新 goal ETA 显示。
3. ✅ 消息级时间戳统一携带时区偏移（`%z`），与工具结果戳一致。
4. 把 T9 时区结果补进论文（已同步摘要段落），大规模数据已在论文中。

## 八、目标模式（自动推进）已落地

参照 Codex `ext/goal` 的续作机制实现：

- **自动续作循环**：目标激活 + 目标模式开启时，一轮结束自动进入下一轮，内部触发
  消息（`internal=true`）只进上下文、不进会话存档、不作为用户气泡渲染。
- **停止条件**（对齐 Codex steering）：目标完成 / 受阻 / 预算耗尽 / 达到轮次上限
  （默认 10）/ 用户 ESC / 模型短句提问（`needs_input`）/ 空输出停滞。停止原因通过
  `AutoTurnEnd` 事件展示在目标卡片（5 秒提示）。
- **打断窗口**：轮间 700ms 留给 ESC；取消不报错，返回最后一轮正常结果。
- **每轮目标上下文**：wire 层目标节新增"你正在自动推进，保持具体进展、不要原地
  循环"规则。
- **UI**：目标卡片新增"目标模式"开关 + "自动推进 n/m · ESC 停止"状态 + 结束原因。
- **设目标即开工**：前端设定目标成功后自动发出第一轮 kickoff（"开始推进当前目标"），
  无需用户再补一条消息；新目标会重置续作计数。
- **轮次上限可调**：目标编辑行新增轮次上限输入（1-100，默认 10），随时可改。
- **记账实时刷新**：每个续作轮结束后重发 GoalUpdate，目标卡片上的 token/耗时
  不落后于实际。
- **kickoff 防漏**：若设目标时上一轮还在处理中，等处理结束后自动补发首轮。
- **暂停/继续**：目标卡片新增暂停按钮（Codex 的 Paused 状态）；暂停即停自动推进，
  继续时重置阻塞审计（fresh audit）。
- **聊天区自动推进芯片**：输入框上方显示"自动推进 n/m · ESC 停止"脉冲徽标，
  右栏收起也能看到推进状态。
- **停止原因修复**：暂停导致的停止此前误报"达到轮次上限"，已补 `goal_paused` 原因；
  burst 结束现在会在聊天区插入系统行（"目标已完成 / 需要你的输入 / 已暂停…"），
  不只出现在右栏。
- **偏好持久化**：目标模式开关存入 settings.json，重启保留用户选择。
- **受阻步骤同步**：模型标记目标 blocked 时，进行中的计划步骤同步转为 blocked
  并附原因，不再悬在半空。

### 端到端验证（真实 API，3 轮）

`benchmarks/goal_advance_probe.py` 用生产格式（harness 系统提示 + goal 节 +
内部续作触发）跑"写 fib.py 并运行验证"目标：

| 轮次 | 触发 | 模型动作 |
|---|---|---|
| 1 | 用户目标 | `update_plan` + `write_file`（先计划再动手） |
| 2 | 自动续作 | `run_shell`（跑脚本，不空转不反问） |
| 3 | 自动续作 | `run_shell` 验证 + 产出 118 字结论 |

结论：自动推进循环有持续燃料；探针还顺手验证了 Anthropic 协议的
`tool_use ↔ tool_result` 配对规则（所有结果必须合并进紧邻的同一用户消息）。

### 全执行模式验证（真实工具执行）

`--execute` 模式真实执行 `write_file` / `run_shell`（临时工作目录），直到目标
标记完成：

| 轮次 | 触发 | 真实动作 |
|---|---|---|
| 1 | 用户目标 | `update_plan` + `write_file`（写出 fib.py） |
| 2 | 自动续作 | `run_shell`（实际运行脚本） |
| 3 | 自动续作 | `update_plan` 收尾 + `update_goal complete`（385 字报告） |

**goal_complete=True，3 轮自动推进完成目标。** 这是对"向着目标自动前进"最直接的
验证：工具真实执行、模型自我验证并主动标记完成，中途无需人工介入。

### 复杂目标健壮性验证

用更难的目标复测（stats.py 模块 + pytest 测试 + 跑测试，最多 8 轮）：

| 轮次 | 动作 |
|---|---|
| 1 | `update_plan` + 环境检查（run_shell） |
| 2 | `write_file` ×2（模块 + 测试）+ run_shell |
| 3-4 | run_shell 迭代（测试失败→修复→重跑） |
| 5 | `update_plan` 收尾 + `update_goal complete` |

**5 轮自动完成**，计划生命周期完整记录：
`检查工作区 → 写 stats.py → 写 pytest → 跑测试并汇报`，四步全部 completed。

**顺带修复一个真实兼容性问题**：模型发 `update_plan` 时用了 Codex 规范的
`step` 字段而非我们的 `content`（训练偏置），真实后端会报
"Missing step content" 让模型重试。已加 `step` 别名兼容（工具描述同步更新，
新增单测），探针确认别名后计划可完整记录。

### 停止条件审计（批量，第 12 节）

`--batch` 模式跑 3 个真实目标（fib.py / stats.py+pytest / count.sh，全部真实
工具执行）：

- **完成率 3/3**，平均 4.7 轮（3 / 6 / 5）。
- **完成前验证足迹**：每个目标在 `update_goal complete` 前都执行过 `run_shell`
  （跑脚本/测试），无一空手完成；stats 目标还经历了
  "写→写→跑→跑→收尾"的两轮测试迭代。
- `needs_input` 启发式零误触发：全程无提前停止。

结论：目标模式在多种任务形态（脚本、模块+测试、shell）下都能自主推进到完成，
且遵守"完成前必须验证"的审计规则。

### 第十三节：前端审计修复

- **kickoff 清空草稿**：自动开工消息此前无条件 `setInput("")`，用户在设定目标后
  400ms 内正在输入框打字会被清空。修复：仅当消息来自输入框才清空 composer，
  kickoff 不再碰用户草稿。
- 新增用户向文档：[目标模式使用指南.md](目标模式使用指南.md)（上手、控制项、
  状态、停止原因、FAQ、实测背书）。
- 论文 5.6 新增 [fig4_goalmode](paper/figures/fig4_goalmode.png)：批量审计的
  完成轮次图。

### 第十四节：真机反馈修复——"设完就跑"

用户实测反馈：设定目标后没有自动推进，需要手动说"继续"。根因是 `needs_input`
启发式对**第一轮（kickoff）**也生效——模型设定目标后的首个回复若以短问句收尾
（如"具体要统计哪些文件？"），burst 在 0 个自动轮次时就停下等待。

修复：问句停止只对**自动续作轮次**生效（`burst > 0`）；kickoff 首轮永不停，
保证目标设定后立即开工。同时保留空输出保护（`stalled`）与轮次上限，避免模型
真正卡住时空转。相关单测同步更新。

### 第十五节：新增"设定为目标"（Codex 交互对齐）

用户反馈：期望像 Codex 一样，把**当前命令**直接提升为目标，然后连续工作到完成，
而不是去任务面板二次输入。已实现：

- 聊天输入框新增 **设定为目标** 按钮：当前输入 → `set_goal_cmd` → 自动切到任务
  面板显示目标卡 → kickoff 立即开工 → 连续推进直至完成。
- 默认轮次上限 10 → **20**：利用 DeepSeek 前缀缓存（实测 ~99% 命中）让续作轮次
  边际成本很低，"直至完成"语义更从容；上限仍是安全网。

### 第十六节：真机复测两处修复

**1. 设定目标后继续了旧工作** → 三管齐下：
- kickoff 首条消息不再泛泛，而是带上目标原文（"开始推进当前目标：<目标>"）；
- 目标 wire 块对**刚设定（2 分钟内）的目标**加醒目提示："JUST SET — 这是当前
  任务，立即切换，不要继续无关的旧工作"（新增单测覆盖新旧目标区分）；
- 旧对话惯性大时，模型有了显式的切换信号。

**2. 暂停后继续卡住** → 根因：继续只改了状态（Paused→Active），没有触发新推进，
agent 在等输入。修复：**继续时自动重新 kickoff**（`handleToggleGoalPause(false)`
成功后触发新一轮自动推进），不再是死等。

### 第十七节：历史持久化 + 表格渲染（用户报告两 bug）

**1. 对话历史"更新后丢失"** → 排查发现：会话其实一直在保存，但存放在
`workspace_path`（用户残留的 Downloads 测试文件夹）下的 `.deepseek-code/sessions`，
而不是与项目/应用绑定——换工作区或重启后历史就"消失"在别处。修复三件套：
- 会话存储迁移到**稳定的应用数据目录**（app data dir / sessions），与工作区解耦；
- 启动时**一次性迁移**旧的 workspace 会话（现有 15 个会话不丢）；
- 启动时**自动恢复当前工作区最近会话**（`restore_last_session`），更新/重启后
  对话直接回来，不再从空白开始。

**2. AI 表格不渲染** → 根因：react-markdown 默认不解析 GFM（表格），项目从未装
`remark-gfm`。已安装并接入 ChatPanel 两处 + MarkdownPreview，补 md 预览表格样式。

### 本轮收尾（第九节进度）

- **计划合并语义可单测**：`update_plan` 的核心合并逻辑抽成纯函数
  `apply_plan_update`（身份保持、时间戳后端独占、最多一个 in_progress、进展重置
  阻塞审计），新增 2 个单测（Rust 15 → 17）。
- **论文案例小节**：目标模式的全执行实测（3 轮自动完成）已写成论文
  `5.6 Goal mode: persistent goals with auto-advance`（paper.tex / paper.md /
  preview.html 同步）。
- 顺手修正论文中一处过时表述（"session-frozen clock"→"clock-free system prompt"）。

### 第十节：审计修复两个真实 bug

1. **无目标时的噪音事件**：原实现在每次普通消息后都会发出
   `AutoTurnEnd("no_goal")`，前端会在每条消息后插入"暂无活跃目标"系统行。
   修复：只有实际跑了 burst（或活跃目标 + 模型提问 `needs_input`）才发结束事件。
2. **目标模式偏好重启回滚**：前端先用持久化的 goalMode 设状态，随后
   `get_goal_mode_cmd` 用后端默认值 true 覆盖——用户关掉的目标模式重启后悄悄
   变回开。修复：初始化时把持久化值同步给后端（`set_goal_mode_cmd`），不再反向
   覆盖；kickoff 也只在目标模式开启时触发。

验证：18 测试全绿，0 警告，构建通过。

验证：Rust 15 测试全绿（含自动推进停止条件单测），0 警告，前端构建通过。
