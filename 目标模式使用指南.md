# 目标模式使用指南

> DeepSeek Code 的目标模式（Goal Mode）让你设定一个**跨轮次持续的目标**，小鲸鱼会
> 在每轮结束后**自动向目标推进**，直到完成、受阻、预算耗尽、轮次上限或你喊停。
> 灵感来自 Codex 的线程目标（thread goal）机制，并叠加了时间感知层：目标与计划
> 步骤都带实时年龄，暂停过久的步骤会自动标记 STALE。

## 快速上手

### 方式一：输入框直接"设定为目标"（推荐）

1. 在聊天输入框里输入目标（例如："把时间感知层测评扩到 100 例并回填论文"）。
2. 点输入框旁的 **设定为目标** 按钮——当前命令立即成为跨轮次目标，小鲸鱼自动
   开工并**连续工作直到完成**（目标模式默认开启）。

### 方式二：任务面板手动设置

1. 打开右侧"任务"面板，在目标编辑框里输入目标，可填 token 预算和自动推进轮次
   上限（默认 20）。
2. 点**设定目标**——同样自动开工。

### 观察与控制
3. 观察：目标卡片显示状态与预算，聊天区显示"自动推进 n/m"脉冲徽标，每轮的工具
   动作以流水线卡片呈现。
4. 小鲸鱼会一直推进到：**目标完成（✓）/ 受阻 / 预算耗尽 / 达到轮次上限 / 你按
   ESC / 它需要你输入**。停止原因会同时显示在聊天区和目标卡片。

## 控制项

| 控件 | 作用 |
|---|---|
| 目标模式开关 | 关闭后不再自动续作（偏好会记住，重启不丢） |
| 暂停 / 继续 | 暂停立即在下一轮边界停止推进；继续时重置受阻审计 |
| 轮次上限（1-100） | 每次用户消息后最多自动推进的轮数（默认 20，DeepSeek 前缀缓存
  让续作轮次很便宜，所以默认可以大方些） |
| ESC | 中断当前轮，并在下一轮边界停止整个推进序列 |
| token 预算（可选） | 耗尽后自动转 budget_limited 并收尾 |

## 状态说明

- **active** 进行中：每轮结束自动续作。
- **paused** 已暂停：不会自动续作，手动继续后恢复。
- **blocked** 受阻：模型连续多轮遇到同一阻塞（3 轮审计）后标记；目标卡片显示
  当前 N/3 进度。
- **budget_limited** 预算耗尽：停止新工作、收尾总结。
- **complete** 已完成：模型必须**验证过**才允许标记（完成审计：逐条对照当前
  状态），不会空手完成。

## 停止原因一览

`✓ 目标已完成` · `目标已受阻` · `预算耗尽` · `达到轮次上限` · `已由你停止`（ESC）·
`目标已暂停` · `需要你的输入`（仅在**自动续作轮次**之后模型以短问句收尾时暂停——
设定目标后的第一轮绝不会因此停下，保证"设完就跑"）。

## 与时间感知层的关系

- 每轮请求尾部注入目标块：目标原文 + 状态 + 已用/剩余 token + 计划（每步带
  `age`，暂停超过保鲜期标记 **STALE——恢复前重新核实工作树**）。
- 步骤 ETA 按模型校准：Flash 高估 3.71×、Pro 低估 0.56×，展示的是校准后的数字。
- 自动续作的触发消息是**内部消息**：只进模型上下文，不进会话存档、不显示为你的
  气泡，恢复会话时不会污染历史。

## 常见问题

- **设了目标没自动开工？** 检查目标模式开关是否开启；如果设定目标时上一轮还在
  处理中，等处理结束后会自动补发首轮。
- **重启后目标还在吗？** 在——目标随会话持久化；目标模式开关偏好也持久化。
- **想换个目标？** 直接在编辑框输入新目标并设定（UI 允许替换；模型侧出于防误触
  会拒绝覆盖未完成目标）。
- **自动推进太激进/太慢？** 调低轮次上限，或直接暂停；每轮之间留有 700ms 的
  打断窗口，ESC 随时可停。

## 实测表现（2026-08，deepseek-v4-flash，真实工具执行）

3 个目标批量审计：**3/3 自动完成**，平均 4.7 轮；每个目标在标记完成前都执行过
验证（运行代码/测试），无空手完成；`needs_input` 零误触发。详细数据见
[时间戳harness测试报告.md](时间戳harness测试报告.md) 与
`benchmarks/goal_advance_batch_result.json`。
