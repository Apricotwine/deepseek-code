# DeepSeek Code 设计审查与优化清单

> 2026-08-11 · 系统性排查 · 状态：✅ 已修 / 🔧 建议 / 📋 已规划

## 一、Agent 流程（正确性）

| # | 发现 | 状态 |
|---|---|---|
| 1 | **上下文占用统计错误**：`context_engine.token_usage` 原先把每次调用的完整
  input+output **累加**，导致 `should_compact` 按"累计消耗 token"而非"当前窗口
  大小"触发——长会话会过早触发软遗忘。现改为直接用 API 返回的
  `input_tokens`（即模型实际看到的实时 prompt 大小），软遗忘时机准确。 | ✅ |
| 2 | **符号链接逃逸**：`resolve_path`（tools.rs 与 agent.rs 各一份）只做字符串
  `starts_with(root)` 检查，工作区内指向外部的 symlink 可绕过。现改为规范化
  父目录后再比较（同时覆盖尚未创建的新文件）。 | ✅ |
| 3 | 重试/退避：429 与 5xx 指数退避重试、取消感知（150ms 轮询）、300s 超时、
  流式失败回退批量——已具备。 | ✅ |
| 4 | 并行工具执行上限 4、结果按声明顺序合并、失败任务用真实 id 应答（协议安全）。 | ✅ |
| 5 | 推理预算（Think/Deep）、过度思考降级（overthinking → downgrade）——已具备。 | ✅ |

## 二、上下文与性能

| # | 发现 | 状态 |
|---|---|---|
| 6 | 系统提示每会话固定（时钟收敛后完全静态），前缀缓存命中实测 ~99%。 | ✅ |
| 7 | **消息列表无虚拟化**：1M 上下文下 DOM 可积累数千条消息，渲染卡顿。
  建议：折叠历史轮次为摘要行 + 惰性渲染（同"任务流水线"的收折思路）。 | 🔧 |
| 8 | 前端 bundle 1.3MB 未分包（vite 警告）。建议按面板/组件做动态 import 分包。 | 🔧 |
| 9 | 无 React Error Boundary：渲染异常会白屏。建议加全局错误边界 + 兜底重载。 | 🔧 |
| 10 | token 估算（CJK 1 char/token，其余 /4）用于监控展示；软遗忘已改用 API 真实值。 | ✅ |

## 三、持久化与数据

| # | 发现 | 状态 |
|---|---|---|
| 11 | 会话已迁移到应用数据目录（与工作区解耦）+ 启动自动恢复最近会话。 | ✅ |
| 12 | 自动保存此前只在轮次结束触发——**新增 30s 心跳 + 页面隐藏时保存**，
  崩溃/关闭不再丢最后一段。 | ✅ |
| 13 | 会话含完整 tool 块（P2 保真度），恢复时时间戳在 wire 层重算（不双标）。 | ✅ |

## 四、目标模式与时间感知

| # | 发现 | 状态 |
|---|---|---|
| 14 | 目标模式（设定为目标、自动推进、暂停/继续、预算、轮次上限、停止原因）已闭环，
  批量实测 3/3 自动完成、完成前必验证。 | ✅ |
| 15 | 时间感知层（单一权威时钟、决策链、per-tool horizon、内容级新鲜度、
  per-model ETA 校准、时区健壮性）已集成并测试（T1-T9 矩阵）。 | ✅ |
| 16 | 建议：决策链提示词已进系统提示，但 `update_plan` 的步骤状态转换决策链
  尚未单独验证（列入基准扩展计划）。 | 📋 |

## 五、安全与合规

| # | 发现 | 状态 |
|---|---|---|
| 17 | API key 只读自系统设置文件，仓库内无硬编码密钥（已扫描）。 | ✅ |
| 18 | `.deepseek-code/`（会话、记忆、临时文件）已加入 .gitignore；个人研究
  `research/` 默认不入库。 | ✅ |
| 19 | 建议：命令层集成测试（无头 Tauri）作为后续 CI 的一部分。 | 📋 |

## 六、测试与验证资产

- Rust 单测 19 个：时间戳注入、计划合并语义、停止条件、协议安全、决策链规则。
- 基准脚本：T1-T9 时间矩阵、goal-advance 全执行探针、缓存命中探针。
- 论文（paper/）+ 使用指南 + 测试报告。

## 结论

核心 agent 流程的**正确性缺陷已清零**（统计、安全、持久化三类），当前最值得投的
是**前端体验层**（虚拟化、分包、错误边界）与**工程化**（CI、集成测试、GitHub 托管）。
